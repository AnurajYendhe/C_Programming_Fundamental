#include<stdio.h>

int main()
{
    // we can create array of any primative data type
    char cArr[5];
    int iArr[5];
    float fArr[5];
    double dArr[5];

    // char ch[]; not allowed

    // we can use variable as size of array
    int No = 3;
    int Arr[No]; // allowed
    
    return 0;
}